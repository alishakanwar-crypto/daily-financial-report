app = "daily-market-pulse"
primary_region = "sin"

[build]

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

[env]
  REPORT_HOUR_IST = "7"
  REPORT_MINUTE_IST = "0"
  SOLAR_REPORT_HOUR_IST = "10"
  SOLAR_REPORT_MINUTE_IST = "0"
  TIMEZONE = "Asia/Kolkata"
  DB_PATH = "/app/data/app.db"
  SOLAR_DB_PATH = "/app/data/solar.db"
  BASE_URL = "https://daily-market-pulse.fly.dev"

[mounts]
  source = "data"
  destination = "/app/data"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
